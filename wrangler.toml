name = "instagame-web"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# These are overridden by secrets set in Cloudflare dashboard
# Set RAZORPAY_KEY_ID and RAZORPAY_KEY_SECRET as secrets via:
# Dashboard → Pages → Settings → Environment Variables → Add variable (encrypted)
